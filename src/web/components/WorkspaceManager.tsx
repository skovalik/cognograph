// Stub for desktop-only public repo. Full implementation in src/web/ (private repo).

interface WorkspaceManagerProps {
  isOpen: boolean
  onClose: () => void
}

export function WorkspaceManager(_props: WorkspaceManagerProps): JSX.Element | null {
  return null
}
